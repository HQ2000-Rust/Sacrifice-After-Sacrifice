use bevy::prelude::*;

pub enum CameraType {
    Menu,
    Level,
}

/*pub fn camera(camera_type: CameraType) -> Camera {
    match camera_type {
        CameraType::Menu => {
            Camera2d:
        }
        CameraType::Level => {

        }
    }
}*/
