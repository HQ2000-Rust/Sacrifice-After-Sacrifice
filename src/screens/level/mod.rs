pub mod one;

use bevy::prelude::*;

pub struct LevelScreensPlugin;

impl Plugin for LevelScreensPlugin {
    fn build(&self, app: &mut App) {
        app.add_plugins((
            
        ))
    }
}
